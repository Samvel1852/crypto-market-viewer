VITE_COINGECKO_API_KEY=your_demo_key_here

# Set to "true" to use MSW mocks for CoinPaprika + CoinGecko (avoids 402 / quota when testing locally). Remove or set false for real APIs.
# VITE_MOCK_API=true
